---
description: Check a screen against the clinical-tool design language
argument-hint: <component-or-page>
---
Review `$1` against CLAUDE.md's design language rules: information density, single typographic
hierarchy, functional-only color use, visible state for every async action, restrained motion.
Flag anything that reads as consumer-app rather than clinical-tool.
